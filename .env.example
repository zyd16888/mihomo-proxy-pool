# 启动前替换为至少 12 字符的独立管理口令。
ADMIN_KEY=
# 替换为项目实际的 GHCR 镜像地址；支持 latest 或固定版本标签。
MIHOMO_IMAGE=ghcr.io/zyd16888/mihomo-proxy-pool:latest
TZ=Asia/Shanghai
# 出口 IP 探测使用的回环端口；与宿主机服务冲突时修改，0 关闭按节点探测。
EXIT_PROBE_PORT=37891
