# 验证记录

日期：2026-09-09。

## 已验证

- Go 1.26.3 / Windows amd64 编译；源码要求 Go 1.25+。
- 17 个测试函数通过，涵盖格式解析、部分错误订阅拒绝、认证与跨站保护、稳定节点引用、独立监听、数据库重启恢复、并发写入、端口冲突、重载/监听验证失败回滚。
- `go test -race ./internal/...` 通过，运行时集成已通过 `MIHOMO_TEST_BIN` 启用。
- `go vet ./...`、`node --check web/static/app.js` 通过。
- `CGO_ENABLED=0` 的 Linux amd64 与 arm64 管理二进制交叉编译通过。
- 使用官方 `mihomo-windows-amd64-compatible` v1.19.30 进行真实内核测试：新增端口、HTTP 请求分别走 A/B 两个本地代理夹具、同端口换绑、内核重启恢复、启停及删除端口。未访问外部目标网站。
- 本地浏览器验证：登录、节点导入、监听创建、固定端口换绑、管理进程重启后重新登录并恢复绑定。390px 宽度下页面无横向溢出，表格可独立滚动，退出入口可见；未发现浏览器 console error/warn。
- 来源文件 UTF-8 无 BOM；Shell 脚本为 LF，Python 事件监听脚本语法与 Supervisor 配置节结构已检查。
- Docker Hub 上 `metacubex/mihomo:v1.19.30` 包含 Linux amd64 / arm64 平台镜像。

## 未验证

本机未安装 Docker，因此没有执行 Docker 镜像构建、Linux host 网络容器启动、Supervisor 进程重启及整容器退出验收。Dockerfile、Compose 和监督配置已实现，但这些静态检查和跨平台二进制构建不能代替运行容器验收。

未接入真实机场订阅、爬虫或 Byparr，也未验证远端节点连通性、出口 IP、CF 过盾成功率或采集吞吐量。

## 本地文件边界

实际工作目录：`D:/project/go_project/mihomo-proxy-main`。原始下载副本没有 Git 历史，后续由用户初始化 Git。未发布镜像。

改造期间曾使用原始快照、外部项目调研副本，以及 `bin/`、`.test-data/` 中的内核、编译产物、测试数据库和日志。这些均为临时验证文件，不属于项目交付，也不构成运行依赖。浏览器验收服务在验证完成后停止。

源码仓库不包含运行数据、订阅密钥或本地测试口令。Docker 部署时会自行初始化持久数据目录。


## 节点延迟与批量检测（2026-09-09）

新增 8 个行为测试，`go test -race ./...` 和 `go vet ./...` 通过。当前共 25 个测试函数，其中真实内核集成测试因本轮未配置 `MIHOMO_TEST_BIN` 而跳过，其余 24 个通过。未修改存储 schema；测试确认检测不会增加配置版本或改写内核配置文件。

本地浏览器使用模拟内核检查成功、超时、排队、取消、筛选范围、刷新保留结果和移动端布局，未发现 console error/warn。模拟服务及临时目录在验证完成后清理，未对线上订阅或节点发起检测。本次变更尚未在部署环境验证。
