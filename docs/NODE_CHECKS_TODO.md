# 节点延迟与批量检测

用户已确认：检测结果仅保存在内存；分流规则作为独立功能。

- [x] 内存检测服务：单节点与批量共用 4 个并发，同节点在途去重。
- [x] 批量后台执行、逐项进度、停止及服务退出取消。
- [x] 延迟、时间与失败状态展示；刷新页面恢复内存结果，节点配置变化后过期。
- [x] 当前筛选结果批量检测；检测时可继续搜索和浏览。
- [x] 行为及竞态测试、页面验证和接口文档。

边界：不写数据库，不增加配置版本，不触发重载；服务重启清空检测结果和任务。


验证：新增 8 个行为测试函数，覆盖全局并发与去重、批量停止、内存状态、配置过期、禁用节点、服务退出、异步 API 和过期结果不复活；`go test -race ./...`、`go vet ./...`、JS 语法与 diff 检查通过。

浏览器使用本地模拟内核验证：筛选 2 个节点检测、刷新后展示、9 节点批量成功/超时、停止取消及 390px 移动端布局。未连接真实订阅或节点；真实内核集成测试本轮未启用。
