# 订阅用量、VLESS 与紧凑节点界面

用户已确认实施；不读取真实节点凭据，不改分流规则。

- [x] 解析 subscription-userinfo，持久化最新有效快照与刷新状态。
- [x] 兼容已有数据库；缺失/异常保留旧快照，换订阅地址后清除旧用量。
- [x] 补全 VLESS TLS/Reality/Vision、指纹、ALPN、UDP 编码等关键参数映射。
- [x] 节点按订阅分组为紧凑卡片，远程地址放详情，支持组内检测。
- [x] 订阅页及分组标题显示用量、剩余、到期与更新时间。
- [x] 数据、解析、同名重导入和 UI 回归；清理临时验证文件。

边界：用量是机场账户快照，更新于订阅响应；节点延迟继续只保存在内存。修复前丢失的参数需要以原名称重新导入原链接恢复。


验证：用量解析/持久化/旧库补表、URL 变更保护、仅用量刷新、VLESS TLS/Reality/Vision、同名重导入等回归通过；官方 Mihomo v1.19.30 校验生成的 Reality 配置通过。完整 race 回归通过，浏览器验证分组、用量刷新不改版本、详情与移动端布局。真实用户节点的连接未验证。

额外修复：取消检测时的 SQLite 短查询完成后再处理取消，避免当前驱动的游标交付与取消竞态导致 Windows 文件句柄未释放；相关用例重复 30 轮通过。
